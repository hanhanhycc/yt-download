fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Pin bcrypt < 4.1: passlib 1.7.4 reads bcrypt.__about__.__version__, which
# bcrypt 4.1+ removed, breaking password hashing.
bcrypt==4.0.1
python-multipart==0.0.12
# Unpinned on purpose: YouTube changes its anti-bot/extraction constantly,
# so each image build should pull the newest yt-dlp.
yt-dlp
email-validator==2.2.0
python-dotenv==1.0.1
